'use strict';

/**
 * resume-builder controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::resume-builder.resume-builder');
